{% extends 'base.html.twig' %}

{% block title %}Lifeform{% endblock %}

{% block body %}
    <h1>Lifeform</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lifeform.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ lifeform.imageName }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lifeform.name }}</td>
            </tr>
            <tr>
                <th>Species</th>
                <td>{{ lifeform.species }}</td>
            </tr>
            <tr>
                <th>Behavior</th>
                <td>{{ lifeform.behavior }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ lifeform.description }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ lifeform.createdAt ? lifeform.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ lifeform.updatedAt ? lifeform.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lifeform_index') }}">back to list</a>

    <a href="{{ path('app_lifeform_edit', {'id': lifeform.id}) }}">edit</a>

    {{ include('lifeform/_delete_form.html.twig') }}
{% endblock %}
