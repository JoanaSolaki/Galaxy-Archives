{% extends 'base.html.twig' %}

{% block title %}New Galaxy{% endblock %}

{% block main %}
{# {% if app.user == null %} #}
    <script>
        window.location.href = "{{ path('app_login') }}";
    </script>
{# {% endif %}

{% if app.user.roles != "ROLE_ADMIN" %}
    <script>
        window.location.href = "{{ path('app_login') }}";
    </script>
{% endif %}

    <h1>Create new Galaxy</h1>

    {{ include('galaxy/_form.html.twig') }}

    <a href="{{ path('app_galaxy_index') }}">back to list</a>
{% endblock %} #}
