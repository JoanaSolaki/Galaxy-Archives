{% extends 'base.html.twig' %}

{% block title %}Edit Galaxy{% endblock %}

{% block body %}

{# {% if app.user == null %} #}
    <script>
        window.location.href = "{{ path('app_login') }}";
    </script>
{# {% endif %} #}
{# 
{% if app.user.roles != "ROLE_ADMIN" %}
    <script>
        window.location.href = "{{ path('app_login') }}";
    </script>
{% endif %}

    <h1>Edit Galaxy</h1>

    {{ include('galaxy/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_galaxy_index') }}">back to list</a>

{% endblock %} #}
