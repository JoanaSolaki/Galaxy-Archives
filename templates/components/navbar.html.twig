<nav class="navbar navbar-expand-md" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('homepage') }}">
        <img src="{{ asset('img/GALAXY_archives.png') }}" alt="logo Galaxy archives">
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
      <ul class="navbar-nav me-auto mb-0 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="{{ path('homepage') }}">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('galaxy') }}">Galaxies</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('planet') }}">Planets</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('lifeform') }}">Lifeforms</a>
        </li>
      </ul>
        <ul class="navbar-nav">
        {% if app.user %}
            <li class="nav-item d-flex">
                <a href="{{ path('user', {'id' : app.user.id}) }}" class="btn" type="button">{{ app.user.username }}</a>
                <a href="{{ path('app_logout') }}" class="btn btn_full" type="button">Logout</a>
            </li>
        {% endif %}

        {% if app.user == null %}
            <li class="nav-item d-flex">
                <a href="{{ path('app_login') }}" class="btn btn_outline" type="button">Sign-in</a>
                <a href="{{ path('app_register') }}" class="btn btn_full nav-link" type="button">Sign-up</a>
            </li>
        {% endif %}
      </ul>
      </div>
  </div>
</nav>